import subprocess
#token = input("Enter your bot's token: ")
subprocess.Popen(['python', 'bot-choose-guild.py'], start_new_session=True)
subprocess.Popen(['python', 'view-messages.py'], start_new_session=True)
#print(pidl.pid)
